# Dependabot: dependency upgrade PRs; does not run in CI
# Applies to this repo only; copy to .github/dependabot.yml in other repos to use
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
